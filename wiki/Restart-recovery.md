# Restart recovery

Future triggers are restored without recalculating from the configured delay. Active sensors are restored until `sensorActiveUntil`; expired active sensors are cleaned.

## Francais

Les declenchements futurs sont restaures sans recalcul depuis le delai configure. Les detecteurs actifs sont restaures jusqu'a `sensorActiveUntil`; les etats expires sont nettoyes.
