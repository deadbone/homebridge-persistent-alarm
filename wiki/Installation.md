# Installation

Install with Homebridge UI or `npm install -g homebridge-persistent-alarm`. Requires Homebridge `^1.6.0 || ^2.0.0` and Node.js `^22.12.0 || ^24.0.0`.

## Francais

Installez via Homebridge UI ou `npm install -g homebridge-persistent-alarm`. Requiert Homebridge `^1.6.0 || ^2.0.0` et Node.js `^22.12.0 || ^24.0.0`.
