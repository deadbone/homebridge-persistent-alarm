# HomeKit exposure

Use `homekitExposure.cancelSwitch` to hide or expose the reset switch. Trigger and motion accessories are mandatory.

## Francais

Utilisez `homekitExposure.cancelSwitch` pour masquer ou afficher le reset. Le trigger et le detecteur sont obligatoires.
