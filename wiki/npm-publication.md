# npm publication

The publish workflow uses npm Trusted Publishing and does not require an npm token in the repository.

## Francais

Le workflow de publication utilise npm Trusted Publishing et ne demande pas de token npm dans le depot.
