# Development

Run `npm install`, `npm run lint`, `npm run build`, and `npm test`. The plugin is TypeScript, strict, ESM, and dynamic-platform based.

## Francais

Lancez `npm install`, `npm run lint`, `npm run build` et `npm test`. Le plugin utilise TypeScript strict, ESM et une plateforme dynamique.
