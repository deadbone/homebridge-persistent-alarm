# Troubleshooting

Check unique IDs, valid repeat mode, positive durations, and `delaySeconds > motionDurationSeconds`. Enable `debug` for operational logs.

## Francais

Verifiez les IDs uniques, le mode de repetition, les durees positives et `delaySeconds > motionDurationSeconds`. Activez `debug` pour plus de logs.
